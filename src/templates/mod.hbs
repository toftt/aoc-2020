use crate::solution::Solution;

{{#each day}}
mod day{{this}};
{{/each}}

pub fn solve_day(day: u32) {
    match day {
        {{#each day}}
        {{this}} => day{{this}}::Day {}.solve(),
        {{/each}}
        day => println!("{} has not been solved yet.", day),
    }
}
